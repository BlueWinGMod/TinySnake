plugins {
    id 'com.android.application'
}

android {
    namespace 'com.tiny.snake'
    compileSdk 33

    defaultConfig {
        applicationId "com.tiny.snake"
        minSdk 3
        targetSdk 26
        versionCode 1
        versionName "1.0"
    }

    lintOptions {
        checkReleaseBuilds false
        abortOnError false
    }

    buildTypes {
        release {
            minifyEnabled true
            shrinkResources false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt')
        }
    }
}
